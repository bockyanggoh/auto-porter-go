package main

import (
	_ "github.com/joho/godotenv/autoload"
)
func main() {
	//var port = ":8080"
	//r := mux.NewRouter();
	////r.Handle("/itinerary", middleware.CheckJwtMiddleware(http.HandlerFunc(api.CreateItineraryApi))).Methods(http.MethodPut)
	////r.Handle("/itinerary", middleware.CheckJwtMiddleware(http.HandlerFunc(api.FindItineraryApi))).Methods(http.MethodGet)
	//r.Use(mux.CORSMethodMiddleware(r))
	//err := http.ListenAndServe(port, r)
	//if err  != nil {
	//	log.Fatal(err);
	//}
	//log.Print("Started Listening at http://localhost"+port)

	//services.BatchTvSeriesScanJob()
}
